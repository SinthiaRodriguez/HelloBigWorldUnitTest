@model WebAppNetCore5.Models.PruebaMuchosMuchos.RelacionPersona

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>RelacionPersona</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PersonaId" class="control-label"></label>
                <select asp-for="PersonaId" class ="form-control" asp-items="ViewBag.PersonaId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PersonaFamiliarId" class="control-label"></label>
                <select asp-for="PersonaFamiliarId" class ="form-control" asp-items="ViewBag.PersonaFamiliarId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Parentezco" class="control-label"></label>
                <select asp-for="Parentezco" class="form-control" asp-items="ViewBag.Parentezcos"></select>
                <span asp-validation-for="Parentezco" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
